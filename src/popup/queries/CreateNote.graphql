mutation CreateNote($url: String!, $note: String!, $scope: String!) {
  create(url: $url, note: $note, scope: $scope) {
    _id
    # author
    url
    note
    share
    scope
    createdAt
    updatedAt
  }
}
